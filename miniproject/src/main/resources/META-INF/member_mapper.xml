<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member.MemberMapper">

<insert id="member_insert">
insert into member (midx,memail,mpasswd,mname,mmobile,age_over14,terms_agree,privacy_agree,marketing_agree,join_date)
values ('0',#{memail},#{mpasswd},#{mname},#{mmobile},#{age_over14},#{terms_agree},#{privacy_agree},#{marketing_agree},now())
</insert>

<select id="member_one" resultType="memberdto">
select * from member where memail=#{memail}
</select>

</mapper>